[package]
name = "neolink"
description = "A standards-compliant bridge to Reolink IP cameras"
version = "0.7.0-beta-20260615"
authors = ["George Hilliard <thirtythreeforty@gmail.com>", "Andrew King <sheepchaan@gmail.com>", "janost - https://github.com/janost", "Marc Höper <marc.hoeper@gmx.de>"]
edition = "2018"
license = "AGPL-3.0-or-later"

[workspace]
members = [
  "crates/*",
]

[dependencies]
anyhow = "1.0.103"
base64 = "0.22.1"
byte-slice-cast = {version = "1.2.3", optional = true}
bytes = "1.12.0"
clap = { version = "4.6.1", features = ["derive", "cargo"] }
crossbeam-channel = {version = "0.5.15", optional = true}
dirs = {version = "6.0.0", optional = true}
env_logger = "0.11.11"
fcm-push-listener = {version = "4.1.1", optional = true}
futures = "0.3.32"
gstreamer = {version = "0.25.2", optional = true}
gstreamer-app = { version = "0.25.2", features = ["v1_20"], optional = true }
gstreamer-rtsp = { version = "0.25.0", features = ["v1_20"], optional = true }
gstreamer-rtsp-server = { version = "0.25.2", features = ["v1_20"], optional = true }
heck = "0.5.0"
log = { version = "0.4.33", features = [ "release_max_level_debug" ] }
md5 = {version = "0.8.0", optional = true}
neolink_core = { path = "crates/core", version = "0.7.0-beta" }
once_cell = "1.21.4"
quick-xml = { version = "0.40.1", features = ["serialize"] }
regex = "1.12.4"
rumqttc = "0.25.1"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
tokio = { version = "1.52.3", features = ["rt-multi-thread", "macros", "io-util", "tracing", "signal"] }
tokio-stream = "0.1.18"
tokio-util = { version = "0.7.18", features = ["full", "tracing"] }
toml = "1.1.2"
uuid = { version = "1.23.4", features = ["v4"] }
validator = {version="0.20.0", features = ["derive"] }

[target.'cfg(not(target_env = "msvc"))'.dependencies]
tikv-jemallocator = "0.7.0"

[features]
default = ["gstreamer"]
gstreamer = [
  "dep:gstreamer",
  "dep:gstreamer-app",
  "dep:gstreamer-rtsp",
  "dep:gstreamer-rtsp-server",
  "dep:byte-slice-cast",
  "dep:crossbeam-channel"
]
pushnoti = [
  "dep:fcm-push-listener",
  "dep:dirs",
  "dep:md5"
]
